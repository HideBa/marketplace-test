name: ci
on:
  workflow_run:
    workflows: [ci_server, ci_wer]
    types: [completed]
jobs:
  ci:
    runs-on: ubuntu-latest
    needs:
      - ci_web
      - ci_server
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: echo
        run: echo "hello!!!"
